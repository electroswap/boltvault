apps/extension/.output/
apps/extension/screenshots/
apps/mobile/android/
apps/mobile/ios/
build/
coverage/
dist/
.DS_Store
e2e-results/
.expo/
.expo-export/
*.log
node_modules/
.out/
pnpm-lock.yaml.lock
.turbo/

# Local one-off rebuild helpers stay out of the repository — except these two,
# which are kept on purpose: they carry the prebuild/signing/launch-check
# knowledge and the whole store-packaging pipeline, which would otherwise live
# on one machine only.
rebuild-*.sh

# Local build configuration: the API origin and the wallet API key a developer
# builds against. `.env.example` is the committed template; `.env` is yours and
# holds a real key, so it never leaves the machine.
.env
.env.local
.env.*.local
!.env.example

# Internal working documents, not published
internal-docs/
