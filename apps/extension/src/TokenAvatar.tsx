/**
 * TokenAvatar — re-exported from @boltvault/design (the face contract).
 * Kept as a thin re-export so the extension has one source of truth for the
 * logo pipeline + avatar (moved in A5). See packages/design/src/TokenAvatar.tsx.
 */
export {
  TokenAvatar,
  pickLogo,
  defaultImageProbe,
  type TokenAvatarProps,
  type LogoProbe,
} from '@boltvault/design'
