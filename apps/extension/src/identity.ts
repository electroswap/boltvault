/** The provider identity lives with the page-provider source (master plan §4.2); the extension re-exports it. */
export { BOLTVAULT_ICON, BOLTVAULT_NAME, BOLTVAULT_PROVIDER_UUID, BOLTVAULT_RDNS } from '@boltvault/protocol'
